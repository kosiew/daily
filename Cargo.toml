[package]
name = "daily"
version = "0.1.0"
edition = "2024"

[dependencies]
cocoa = "0.25"
objc = "0.2"
rusqlite = { version = "0.31", features = ["bundled"] }
chrono = { version = "0.4", default-features = false, features = ["std"] }
